version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/sphinx/conf.py

# Python dependencies
python:
  install:
    - requirements: docs/sphinx/requirements.txt

commands:
  - doxygen docs/Doxyfile

        
